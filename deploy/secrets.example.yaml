# Example only. Do not apply this file as-is and never commit real credentials.
# Create these Secrets in namespace `chart` before `scripts/deploy_apply.sh --apply`.
apiVersion: v1
kind: Secret
metadata:
  name: chart-turbopuffer
  namespace: chart
type: Opaque
stringData:
  credential: REPLACE_WITH_TURBOPUFFER_KEY
---
apiVersion: v1
kind: Secret
metadata:
  name: chart-gateway
  namespace: chart
type: Opaque
stringData:
  LAYER_GATEWAY_API_KEY: REPLACE_WITH_LAYER_GATEWAY_API_KEY
